# Leave blank for the local in-memory demo.
# Never commit a real CockroachDB URL or runtime token.
VITE_RUNTIME_MODE=demo
VITE_COCKROACH_DATABASE_URL=

# Used only by the AWS Lambda runtime. Store DATABASE_URL in AWS Secrets Manager
# for deployed environments instead of putting it in a local file.
DATABASE_URL=
DATABASE_URL_SECRET_ARN=
RUNTIME_WRITE_TOKEN=
ARTIFACT_BUCKET=

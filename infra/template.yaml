AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: FlowGrid Memory Runtime hackathon demo

Parameters:
  DatabaseUrl:
    Type: String
    NoEcho: true
    Description: CockroachDB connection URL for the synthetic demo. Lambda encrypts it at rest.
  RuntimeWriteToken:
    Type: String
    NoEcho: true
    MinLength: 24
    Description: Token required for Lambda mutation requests. Keep this out of the public web client.

Globals:
  Function:
    Runtime: nodejs22.x
    Timeout: 15
    MemorySize: 512
    Architectures:
      - arm64

Resources:
  DemoArtifactsBucket:
    Type: AWS::S3::Bucket
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: AES256
      LifecycleConfiguration:
        Rules:
          - Id: expire-synthetic-traces
            Status: Enabled
            ExpirationInDays: 7
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 1

  MemoryRuntimeApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: demo
      Cors:
        AllowMethods: "'GET,POST,OPTIONS'"
        AllowHeaders: "'content-type,x-runtime-token'"
        AllowOrigin: "'*'"

  MemoryRuntimeFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: ../lambda/
      Handler: app.handler
      Environment:
        Variables:
          ARTIFACT_BUCKET: !Ref DemoArtifactsBucket
          DATABASE_URL: !Ref DatabaseUrl
          RUNTIME_WRITE_TOKEN: !Ref RuntimeWriteToken
      Policies:
        - S3WritePolicy:
            BucketName: !Ref DemoArtifactsBucket
      Events:
        RuntimeRoot:
          Type: Api
          Properties:
            RestApiId: !Ref MemoryRuntimeApi
            Path: /runtime
            Method: ANY
        RuntimeProxy:
          Type: Api
          Properties:
            RestApiId: !Ref MemoryRuntimeApi
            Path: /runtime/{proxy+}
            Method: ANY

  MemoryRuntimeLogGroup:
    Type: AWS::Logs::LogGroup
    DeletionPolicy: Delete
    UpdateReplacePolicy: Delete
    Properties:
      LogGroupName: !Sub "/aws/lambda/${MemoryRuntimeFunction}"
      RetentionInDays: 7

Outputs:
  RuntimeApiUrl:
    Description: Memory Runtime API endpoint.
    Value: !Sub "https://${MemoryRuntimeApi}.execute-api.${AWS::Region}.amazonaws.com/demo/runtime/"
  DemoArtifactsBucketName:
    Description: S3 bucket holding synthetic run traces.
    Value: !Ref DemoArtifactsBucket

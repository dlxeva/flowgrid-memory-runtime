# FlowGrid Memory Runtime Video Narration

This script matches the editable 90-second ChatCut timeline. It describes the
verified local lifecycle and live synthetic CockroachDB Cloud data. Do not add
an AWS production claim until the reviewed stack is deployed and tested.

## Voice replacement

The current export is intentionally text-led. To add the owner's cloned voice,
register the approved reference sample with the chosen TTS provider and replace
the visual timeline with the generated clips below. Use one clip per section
and preserve the listed frame ranges at 30 fps.

| Frames | Time | Narration |
| --- | --- | --- |
| 0-239 | 0:00-0:08 | Agents remember conversations, but a retrieved request should not silently overwrite a human commitment. |
| 240-659 | 0:08-0:22 | This confirmed judgment is protected by a rationale and a reversal condition. KOL-heavy promotion stays out until conversion evidence exists. |
| 660-1079 | 0:22-0:36 | A new KOL request does not rewrite the project. The runtime creates pending revision P-zero-zero-one, while D-zero-zero-one stays confirmed. |
| 1080-1499 | 0:36-0:50 | Evidence changes authorization, not chat history. The revision can apply only when the recorded condition is satisfied. |
| 1500-1919 | 0:50-1:04 | CockroachDB Cloud persists synthetic sources, judgments, revisions, evidence links, handoffs, audit events, and vector-searchable memory. Managed MCP provides scoped read evidence. |
| 1920-2339 | 1:04-1:18 | When qualifying evidence arrives, D-zero-zero-two becomes current. D-zero-zero-one remains traceable as superseded history. |
| 2340-2699 | 1:18-1:30 | The judgment lifecycle and CockroachDB memory layer are live. The AWS deployment remains pending, and this demo does not claim a public AWS runtime. |

## Delivery boundary

- The narration must not claim that Lambda, API Gateway, Secrets Manager, or
  S3 have been deployed.
- The final Devpost submission still needs a deployed AWS integration, a public
  functional demo URL, and an updated video that shows that deployed path.

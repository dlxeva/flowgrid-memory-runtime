node_modules/
dist/
.env
.env.local
.DS_Store
coverage/
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# SAM build output and per-machine deployment parameters may contain local paths
# or secrets. Deployment configuration is supplied explicitly at deploy time.
.aws-sam/
samconfig.toml
samconfig.local.toml
deploy.env
deploy.local.env

# Owner-provided voice references and local TTS generations.
voiceover/

# Local generation requests and transient render metadata.
.cache/
